#include <stdio.h>
int main()
{
  // int age = 20;
  // printf("age is %d", age); 

  // float pi = 3.14;
  // printf("Value is %f", pi);

  char star = '*';
  printf("Star is %c", star);

      // float pi = 3.14;
      // char hastage = '#';
      return 0;
}