// Take a number(n) from user & output its cubs(n*n*n).

#include <stdio.h>
int main()
{
  int n;

  printf("Enter a number:");

  scanf("%d", &n);

  printf("The cube of : %d", n * n * n);
}
